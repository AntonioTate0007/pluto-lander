
# Pluto Launcher Dashboard

React + Vite + Tailwind dashboard styled like a dark Braiins deck.

```bash
cd dashboard
npm install
npm run dev
```

It expects the FastAPI backend on port 8000 of the same host.
